AUTH_SECRET=replace-with-a-long-random-string
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional — without this, Lectura still builds study packs from slides using a local extractor.
OPENAI_API_KEY=

# Optional — without Stripe keys, billing uses a local demo upgrade.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_PRICE_STUDENT=
STRIPE_PRICE_PRO=
